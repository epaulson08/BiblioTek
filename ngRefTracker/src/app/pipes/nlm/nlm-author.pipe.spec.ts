import { NlmAuthorPipe } from './nlm-author.pipe';

describe('NlmAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new NlmAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
