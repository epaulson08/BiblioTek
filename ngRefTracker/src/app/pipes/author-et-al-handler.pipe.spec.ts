import { AuthorEtAlHandlerPipe } from './author-et-al-handler.pipe';

describe('AuthorEtAlHandlerPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorEtAlHandlerPipe();
    expect(pipe).toBeTruthy();
  });
});
