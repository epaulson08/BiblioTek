import { ApaCitationAuthorsPipe } from './apa-citation-authors.pipe';

describe('ApaCitationAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ApaCitationAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
