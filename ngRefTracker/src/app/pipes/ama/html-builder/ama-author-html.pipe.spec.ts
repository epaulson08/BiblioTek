import { AmaAuthorHtmlPipe } from './ama-author-html.pipe';

describe('AmaAuthorHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new AmaAuthorHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
