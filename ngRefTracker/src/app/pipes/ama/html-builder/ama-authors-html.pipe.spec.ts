import { AmaAuthorsHtmlPipe } from './ama-authors-html.pipe';

describe('AmaAuthorsHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new AmaAuthorsHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
