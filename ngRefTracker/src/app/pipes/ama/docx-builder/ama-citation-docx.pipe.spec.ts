import { AmaCitationDocxPipe } from './ama-citation-docx.pipe';

describe('AmaCitationDocxPipe', () => {
  it('create an instance', () => {
    const pipe = new AmaCitationDocxPipe();
    expect(pipe).toBeTruthy();
  });
});
