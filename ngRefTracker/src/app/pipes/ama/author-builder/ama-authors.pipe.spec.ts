import { AmaAuthorsPipe } from './ama-authors.pipe';

describe('AmaAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new AmaAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
