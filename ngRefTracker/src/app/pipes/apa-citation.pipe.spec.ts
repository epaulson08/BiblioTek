import { ApaCitationPipe } from './apa-citation.pipe';

describe('ApaCitationPipe', () => {
  it('create an instance', () => {
    const pipe = new ApaCitationPipe();
    expect(pipe).toBeTruthy();
  });
});
