import { MlaAuthorPipe } from './mla-author.pipe';

describe('MlaAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new MlaAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
