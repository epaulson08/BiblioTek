import { MlaAuthorsPipe } from './mla-authors.pipe';

describe('MlaAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new MlaAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
