import { ApaAuthorsPipe } from './apa-authors.pipe';

describe('ApaAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ApaAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
