import { ApaCitationDocxPipe } from './apa-citation-docx.pipe';

describe('ApaCitationDocxPipe', () => {
  it('create an instance', () => {
    const pipe = new ApaCitationDocxPipe();
    expect(pipe).toBeTruthy();
  });
});
