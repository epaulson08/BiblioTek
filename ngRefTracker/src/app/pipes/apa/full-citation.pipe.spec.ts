import { FullCitationPipe } from './full-citation.pipe';

describe('FullCitationPipe', () => {
  it('create an instance', () => {
    const pipe = new FullCitationPipe();
    expect(pipe).toBeTruthy();
  });
});
