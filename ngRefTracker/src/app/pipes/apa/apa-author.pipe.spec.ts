import { ApaAuthorPipe } from './apa-author.pipe';

describe('ApaAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new ApaAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
