import { IeeeAuthorHtmlPipe } from './ieee-author-html.pipe';

describe('IeeeAuthorHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new IeeeAuthorHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
