import { IeeeAuthorsPipe } from './ieee-authors.pipe';

describe('IeeeAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new IeeeAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
