import { IeeeCitationDocxPipe } from './ieee-citation-docx.pipe';

describe('IeeeCitationDocxPipe', () => {
  it('create an instance', () => {
    const pipe = new IeeeCitationDocxPipe();
    expect(pipe).toBeTruthy();
  });
});
