import { LocalStorageKeyList } from './local-storage-key-list';

describe('LocalStorageKeyList', () => {
  it('should create an instance', () => {
    expect(new LocalStorageKeyList()).toBeTruthy();
  });
});
