import { CitationStyleLink } from './citation-style-link';

describe('CitationStyleLink', () => {
  it('should create an instance', () => {
    expect(new CitationStyleLink()).toBeTruthy();
  });
});
