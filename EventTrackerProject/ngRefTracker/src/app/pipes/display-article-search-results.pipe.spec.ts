import { DisplayArticleSearchResultsPipe } from './display-article-search-results.pipe';

describe('DisplayArticleSearchResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayArticleSearchResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
